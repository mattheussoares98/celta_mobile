import 'package:flutter/material.dart';

class InsertOrUpdateResearchPrice extends StatelessWidget {
  const InsertOrUpdateResearchPrice({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text("centro"),
      ),
    );
  }
}
