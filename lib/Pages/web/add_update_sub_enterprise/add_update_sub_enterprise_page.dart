import 'package:flutter/material.dart';

class AddUpdateSubEnterprisePage extends StatelessWidget {
  const AddUpdateSubEnterprisePage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Add Update Sub Enterprise'),
      ),
      body: Center(
        child: Text('Add Update Sub Enterprise Page'),
      ),
    );
  }
}
