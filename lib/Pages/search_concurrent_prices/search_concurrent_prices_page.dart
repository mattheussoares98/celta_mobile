import 'package:flutter/material.dart';

class SearchConcurrentPricePage extends StatelessWidget {
  const SearchConcurrentPricePage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text("PESQUISA DE PREÇO NOS CONCORRENTES"),
      ),
    );
  }
}
